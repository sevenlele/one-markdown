{
  "identifier": "default",
  "description": "Default capabilities for OneMarkdown",
  "windows": ["main"],
  "permissions": [
    "dialog:default",
    "shell:default",
    "clipboard-manager:default"
  ]
}
